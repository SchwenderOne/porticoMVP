{
  "name": "portico-mvp-root",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "cd web && npm ci",
    "build": "cd web && npm run build",
    "start": "npm run build && cd web && npm run preview -- --host 0.0.0.0 --port ${PORT:-3000}"
  }
}


